apiVersion: v1
kind: ConfigMap
metadata:
  name: myapp-config
  namespace: myapp
data:
  APP_NAME: "MyKubernetesApp"
  APP_ENV: "production"
  APP_VERSION: "1.0.0"
  DB_HOST: "mysql-service.myapp.svc.cluster.local"
  LOG_LEVEL: "info"
